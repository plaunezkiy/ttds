# load ranked results
# extract top ranking n_d document ids
    # load documents from ids and preprocess
    # concat the content together
    # recompute tfidf for every token
    # report top n_t tokens
# apply ^ to all queries
